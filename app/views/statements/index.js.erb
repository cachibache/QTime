$('#vote').click(function() {
  $(this).replaceWith('');


  // $(this).replaceWith('<%= j link_to "Next", Statement.find_unvoted(current_user).first');
  // $('#statement').load('<%= j Statement_find_unvoted(current_user) %>');
});